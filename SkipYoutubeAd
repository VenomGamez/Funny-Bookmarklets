javascript:(function() {     var skipAdButton = document.querySelector('.ytp-ad-skip-button');     if (skipAdButton) {         skipAdButton.click();     } else {         var overlayAd = document.querySelector('.ytp-ad-overlay-close-button');         if (overlayAd) {             overlayAd.click();         } else {             var video = document.querySelector('video');             if (video && !video.paused) {                 video.currentTime = video.duration;             } else {                 var playButton = document.querySelector('.ytp-play-button');                 if (playButton) {                     playButton.click();                 }             }         }     } })();
